# 赚了么 - 微信记账小程序

一款帮助用户记录彩票、刮刮乐、麻将收支的微信小程序，支持全用户排行榜功能。

## 功能特性

- 📊 **三种记账类型**：彩、刮、麻
- 📝 **记录管理**：支持新增、编辑、删除记录
- 📈 **个人统计**：年度总盈亏、分类盈亏、最近记录
- 🔮 **每日运势**：星座运势分析、幸运数字、开运色
- 🏆 **全用户排行榜**：总排行、分类排行
- 👤 **个人中心**：自定义昵称、头像、生日
- 🎨 **个性化设置**：自定义赢色/亏色（8种颜色可选）
- 📤 **分享功能**：分享给朋友、分享到朋友圈
- ☁️ **微信云开发**：无需服务器，免费部署

## 技术栈

- 微信小程序原生开发
- 微信云开发（云函数 + 云数据库 MongoDB）
- Node.js 云函数

## 项目结构

```
earned-or-not/
├── app.js                 # 小程序入口（需配置环境ID）
├── app.json               # 全局配置
├── app.wxss               # 全局样式
├── cloud/                 # 云函数
│   └── functions/
│       ├── user/          # 用户信息管理
│       ├── lottery/       # 彩票记录（增删改查）
│       ├── scratch/       # 刮刮乐记录（增删改查）
│       ├── mahjong/       # 麻将记录（增删改查）
│       ├── stats/         # 统计查询
│       └── rank/          # 排行榜
├── pages/                 # 小程序页面
│   ├── index/             # 首页（快速记账、最近记录、运势入口）
│   ├── record/            # 记账页面（支持编辑模式）
│   ├── stats/             # 统计页面
│   ├── rank/              # 排行榜（总榜+分类榜）
│   ├── profile/           # 我的（个人资料设置）
│   ├── settings/          # 设置页面（颜色、生日设置）
│   ├── record-list/       # 分类记录列表
│   └── fortune/           # 每日运势
├── images/                # 图片资源
│   ├── logo.png           # 小程序logo
│   └── avatar.png         # 默认头像
└── README.md
```

## 部署步骤

### 1. 创建云开发环境
- 登录微信开发者工具
- 创建小程序项目
- 开通云开发，记录环境ID

### 2. 配置环境ID
修改 `app.js`：
```javascript
wx.cloud.init({
  env: '你的环境ID',
  traceUser: true
});
```

### 3. 部署云函数
在微信开发者工具中：
1. 展开 `cloud/functions/`
2. 右键每个云函数文件夹
3. 选择 **"创建并部署：云端安装依赖"**
4. 依次部署6个云函数：
   - `user`
   - `lottery`
   - `scratch`
   - `mahjong`
   - `stats`
   - `rank`

### 4. 创建数据库集合
在云开发控制台 → 数据库，创建以下集合：
- `users` - 用户信息（_openid, nickname, avatarUrl, createTime, updateTime）
- `lottery` - 彩票记录（_openid, cost, winAmount, createTime, updateTime）
- `scratch` - 刮刮乐记录（_openid, cost, winAmount, createTime, updateTime）
- `mahjong` - 麻将记录（_openid, amount, createTime, updateTime）

**权限设置**：所有集合选择 **"仅创建者可读写"**

### 5. 配置图片资源
将 `images/logo.png` 上传到云存储，获取 fileID 并更新到项目配置中。

### 6. 运行测试
点击预览，测试各项功能。

## 核心功能说明

### 记账流程
1. 首页点击快速记账按钮（彩票/刮刮乐/麻将）
2. 输入花费金额和中奖金额（麻将选择赢/输）
3. 实时预览盈亏金额
4. 保存后自动更新统计

### 记录编辑
- 点击首页最近记录，弹出操作菜单
- 支持编辑和删除
- 编辑时自动带出原有数据

### 排行榜规则
- **总排行**：彩票盈亏 + 刮刮乐盈亏 + 麻将盈亏
- **分类排行**：单独计算各类型的盈亏
- 按盈亏金额从高到低排序
- 显示用户头像和昵称

### 用户系统
- 首次记账自动创建用户记录
- 默认昵称为"赚了么用户"
- 可在"我的"页面修改昵称和头像
- 支持自定义赢色/亏色（8种颜色可选）

### 分享功能
- 首页分享：展示个人盈亏记录
- 排行榜分享：邀请好友参与排名
- 支持分享给朋友和朋友圈

## 数据库设计

### users 集合
```json
{
  "_openid": "用户唯一标识",
  "nickname": "用户昵称",
  "avatarUrl": "头像云存储地址（兼容旧版）",
  "customAvatarUrl": "自定义头像云存储地址",
  "createTime": "创建时间",
  "updateTime": "更新时间"
}
```

### lottery / scratch 集合
```json
{
  "_openid": "用户唯一标识",
  "cost": "花费金额（元）",
  "winAmount": "中奖金额（元）",
  "createTime": "创建时间",
  "updateTime": "更新时间"
}
```

### mahjong 集合
```json
{
  "_openid": "用户唯一标识",
  "amount": "盈亏金额（正为赢，负为输）",
  "createTime": "创建时间",
  "updateTime": "更新时间"
}
```

## 开发团队

- 产品 & 设计：lrp1223
- 开发：18million (AI Assistant)

## 更新日志

### v2.1.2 (2026-03-20)
- 新增：每日运势功能，支持星座运势分析
- 新增：生日设置，自动计算星座
- 新增：分类记录详情页，支持分页加载
- 优化：排行榜分页加载，提升性能
- 优化：敏感词替换（彩票→彩，刮刮乐→刮）
- 修复：星座计算算法
- 修复：记录列表颜色应用用户设置

### v1.2.0 (2026-03-15)
- 新增：设置页面，支持自定义赢色/亏色（8种颜色可选）
- 新增：小程序分享功能（分享给朋友、分享到朋友圈）
- 优化：首页、战绩、统计、排行均应用用户自定义颜色
- 优化：排行榜头像使用云存储链接，解决微信头像过期问题
- 优化：头像本地缓存，减少重复加载
- 修复：云存储 fileID 自动转换为 HTTPS URL

### v1.1.0 (2026-03-06)
- 重构：好友系统改为全用户排行榜
- 新增：个人中心页面（设置昵称、头像）
- 新增：记录编辑和删除功能
- 新增：总排行榜
- 优化：首页样式和交互
- 优化：自定义操作菜单样式
- 修复：新用户自动创建记录
- 修复：删除权限检查

### v2.2.0 (2026-03-26)
- 新增：拱猪游戏（2v2组队版）
- 新增：趣玩页面，集成5款小游戏
- 拱猪规则：猪羊定队、分数平均、满红+200、大满贯翻倍
- 拱猪出牌：♠J先出，横屏布局

### v1.0.0 (2026-03-05)
- 基础功能完成
- 支持彩、刮、麻记账
- 个人统计页面
- 好友系统

## 🎮 趣玩游戏

### 拱猪（2v2组队版）

经典4人纸牌游戏，2v2组队玩法。

**特殊牌分值：**
| 牌 | 分值 |
|---|---|
| ♠Q 猪 | -100 |
| ♦J 羊 | +100 |
| ♣10 变压器 | ×2 或 +50 |
| ♥ 红桃 | -10至-50 |

**出牌规则：**
- ♠J 先出，第一轮必须出 ♠J
- 跟花色，无则垫牌
- 同花色最大者赢本轮

**组队规则：**
- 猪羊定队：猪羊同一人=跟对家组队，不同人=这两人组队
- 队友分数相加后平分

**特殊计分：**
- 满红（13张红桃）：+200分
- 大满贯（猪羊红桃全收）：猪+100，羊+100，红桃+200，变压器再×2

## License

MIT
